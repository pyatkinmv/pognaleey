spring:
  datasource:
    url: jdbc:tc:postgresql:17-alpine:///pognaleey
  profiles:
    active: test
image-search-client:
  base-url: url
  api-key: apiKey
  folder-id: folderId
  permits-per-second: 100
gpt-client:
  # noinspection SpringBootApplicationYaml
  current: null
  yandex:
    base-url: url
    model-uri: uri
    folder-id: folderId
    api-key: apiKey
  openai:
    base-url: url
    model: model
    api-key: apiKey
jwt:
  secret: tests-environment-secret-key-for-jwt-auth