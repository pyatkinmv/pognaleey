spring:
  datasource:
    url: jdbc:tc:postgresql:17-alpine:///pognaleey
  profiles:
    active: test
image-client:
  search:
    openverse:
      base-url: url
  generate:
    kandinsky:
      base-url-get: get-url
      base-url-post: post-url
      api-key: apiKey
      api-secret: apiSecret
gpt-client:
  openai:
    base-url: url
    model: model
    api-key: apiKey
jwt:
  secret: tests-environment-secret-key-for-jwt-auth
app:
  domain-name: https://domain.name